import { TooltipDesignTokens } from '@primeuix/themes/types/tooltip';
import { TooltipPassThrough } from 'primeng/types/tooltip';

export const TooltipStyle: TooltipDesignTokens = {
  root: {
    borderRadius: '1rem',
  },
};

export const TooltipPT: TooltipPassThrough = {
  arrow: {
    class: 'hidden',
  },
};
